version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:current  # Use a full-featured image that supports apt-get
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo Hello, World!"
      - run:
          name: "Create workspace directory"
          command: mkdir -p workspace  # Create the workspace directory
      - persist_to_workspace:
          root: ./workspace  # Persist workspace directory here
          paths:
            - .  # Persist everything in the current directory (if needed)

  sonarqube:
    docker:
      - image: cimg/base:current  # Use a full-featured image that supports apt-get
    resource_class: small
    steps:
      - checkout  # Ensure the code is available in this job
      - attach_workspace:
          at: ./workspace  # Attach the workspace to a custom directory
      - run:
          name: "Install findutils and SonarScanner"
          command: |
            sudo apt-get update && \
            sudo apt-get install -y findutils openjdk-11-jdk wget  # Install find, Java, and wget
            # Download and install SonarScanner
            wget https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-4.8.0.2856-linux.zip -O sonar-scanner.zip
            unzip sonar-scanner.zip
            sudo mv sonar-scanner-4.8.0.2856-linux /opt/sonar-scanner
            sudo ln -s /opt/sonar-scanner/bin/sonar-scanner /usr/local/bin/sonar-scanner
      - run:
          name: "Find files"
          command: "find ./workspace"  # Search in the attached workspace directory
      - run:
          name: "Run SonarQube scanner"
          command: |
            SONAR_BRANCH="${CIRCLE_BRANCH:-master}"
            echo "Sonar branch value is : $SONAR_BRANCH"
            echo "Sonar org value is: $SONAR_ORG"
            sonar-scanner \
              -Dsonar.projectKey="$SONAR_PROJECT_KEY" \
              -Dsonar.organization="$SONAR_ORG" \
              -Dsonar.host.url=https://sonarcloud.io \
              -Dsonar.exclusions="**/android/**,**/ios/**" \
              -Dsonar.login="$SONAR_TOKEN" \
              -Dsonar.branch.name="$SONAR_BRANCH" \
              -Dsonar.sources="./workspace"  # Use the correct directory for sources

workflows:
  version: 2
  say-hello-workflow:
    jobs:
      - say-hello
      - sonarqube:
          requires:
            - say-hello  # Ensure that 'sonarqube' runs after 'say-hello'
